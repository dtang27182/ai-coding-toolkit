{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AI Coding Toolkit Implementation Dataflow",
  "description": "Structured representation of an Implementation Dataflow narrative for visualization and evaluation.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "stage",
    "classes",
    "components",
    "staticData",
    "relationships"
  ],
  "properties": {
    "schemaVersion": {
      "const": 13
    },
    "stage": {
      "description": "The activity that produced this implementation dataflow.",
      "enum": [
        "high-level-design",
        "code-review"
      ]
    },
    "userFlows": {
      "description": "Optional user flows that give context for the implementation dataflow.",
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/userFlowSet"
      }
    },
    "variableExposureCount": {
      "description": "Derived total; null when any inventory is unknown.",
      "type": [
        "integer",
        "null"
      ],
      "minimum": 0
    },
    "changedClassCount": {
      "description": "Derived number of added, modified, or deleted classes.",
      "type": "integer",
      "minimum": 0
    },
    "changedMethodCount": {
      "description": "Derived number of added, modified, or deleted methods in changed classes.",
      "type": "integer",
      "minimum": 0
    },
    "changedComponentCount": {
      "description": "Derived number of added, modified, or deleted UI, system input/output, and external dependency components.",
      "type": "integer",
      "minimum": 0
    },
    "changedDataflowRelationshipCount": {
      "description": "Derived number of added, modified, or deleted dataflow relationships.",
      "type": "integer",
      "minimum": 0
    },
    "changedStateUpdateRelationshipCount": {
      "description": "Derived number of added, modified, or deleted state-update relationships.",
      "type": "integer",
      "minimum": 0
    },
    "classes": {
      "description": "Implementation classes represented by the document. Class, component, and static-data names must be unique across the document.",
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/class"
      }
    },
    "components": {
      "description": "UI surfaces, system inputs and outputs, and external dependencies at the implementation boundary. Use an empty array when none are represented.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/component"
      }
    },
    "staticData": {
      "description": "Fixed data read by represented methods, such as constants or static lookup data. Use an empty array when none are represented. External files and storage remain external-dependency components.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/staticData"
      }
    },
    "relationships": {
      "description": "Direct relationships between represented entities. Preserve intermediate participants. Use state relationships for class-owned state reads and writes, and dataflow relationships for external storage access. Relationships express dependencies and data movement, not execution order.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/relationship"
      }
    }
  },
  "$defs": {
    "changeType": {
      "description": "How an entity or relationship differs from the existing architecture. Use added for a new entry, modified for a changed declaration, responsibility, contract, or relationship, deleted for a removed entry, and unchanged for an existing entry reused without such a change. Participation or a new connection alone does not modify an endpoint.",
      "enum": [
        "added",
        "modified",
        "deleted",
        "unchanged"
      ]
    },
    "userFlow": {
      "description": "Whether this entry lies on a documented user-flow path from its trigger to persisted state or an output. Setup and ownership alone do not qualify.",
      "type": "boolean"
    },
    "userFlowSet": {
      "description": "One user flow with its numbered steps.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name",
        "steps"
      ],
      "properties": {
        "name": {
          "description": "Names this user flow; unique across user flows.",
          "type": "string",
          "minLength": 1
        },
        "steps": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/userFlowStep"
          }
        }
      }
    },
    "userFlowStep": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "text"
      ],
      "properties": {
        "id": {
          "description": "The step's position within its user flow, starting at 1.",
          "type": "integer",
          "minimum": 1
        },
        "text": {
          "description": "The user-flow step. Conditional branches and loops stay in this text.",
          "type": "string",
          "minLength": 1
        }
      }
    },
    "component": {
      "description": "A UI surface, system input or output, or external dependency. Represent implementation handlers and adapters as classes and methods instead.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name",
        "description",
        "type",
        "changeType"
      ],
      "properties": {
        "userFlow": {
          "$ref": "#/$defs/userFlow"
        },
        "name": {
          "description": "Unique across classes, components, and static data; referenced by relationship endpoints.",
          "type": "string",
          "minLength": 1
        },
        "description": {
          "description": "What the component is or represents, including a URL, file path, storage key, or other concrete identifier when available.",
          "type": "string",
          "minLength": 1
        },
        "type": {
          "oneOf": [
            {
              "const": "ui-component",
              "description": "A UI surface that receives user input or presents output."
            },
            {
              "const": "system-input",
              "description": "Data supplied to this system by another system or workflow. May only be the source of a dataflow."
            },
            {
              "const": "system-output",
              "description": "A final result sent to another system or workflow. May only be the target of a dataflow."
            },
            {
              "const": "external-dependency",
              "description": "An external service, file, datastore, or other I/O endpoint used by the implementation."
            }
          ]
        },
        "changeType": {
          "$ref": "#/$defs/changeType"
        }
      }
    },
    "staticData": {
      "description": "Fixed data that a represented method reads without updating. Its name is unique across classes, components, and static data.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name",
        "changeType"
      ],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1
        },
        "changeType": {
          "$ref": "#/$defs/changeType"
        },
        "userFlow": {
          "$ref": "#/$defs/userFlow"
        }
      }
    },
    "class": {
      "type": "object",
      "additionalProperties": false,
      "description": "An implementation class represented by the document.",
      "required": [
        "name",
        "changeType",
        "methods",
        "stateVariables"
      ],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1
        },
        "changeType": {
          "$ref": "#/$defs/changeType"
        },
        "methods": {
          "description": "Methods represented for this class. Method names must be unique within the class.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/method"
          }
        },
        "stateVariables": {
          "description": "Mutable instance fields whose declaration or architectural meaning is added, modified, or deleted, plus unchanged mutable fields referenced by a state-read or state-update relationship. Excludes static or class variables, local variables, unrelated fields, explicitly read-only fields, fields initialized at declaration or construction but never written afterward, and fields used only as references to owned child objects. Constructor assignment is initialization rather than a state update. Runtime writes do not by themselves make an existing field modified. State-variable names must be unique within the class.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/stateVariable"
          }
        },
        "variableExposureCount": {
          "description": "Derived class count; null when its inventory is unknown.",
          "type": [
            "integer",
            "null"
          ],
          "minimum": 0
        },
        "variableExposure": {
          "description": "Existing declarations exposed to this class's changes; null when the inventory is required but not yet known.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/$defs/exposedVariable"
          }
        }
      },
      "allOf": [
        {
          "if": {
            "properties": {
              "changeType": {
                "const": "unchanged"
              }
            },
            "required": [
              "changeType"
            ]
          },
          "then": {
            "properties": {
              "methods": {
                "type": "array",
                "items": {
                  "type": "object",
                  "properties": {
                    "changeType": {
                      "const": "unchanged"
                    }
                  }
                }
              },
              "stateVariables": {
                "type": "array",
                "items": {
                  "type": "object",
                  "properties": {
                    "changeType": {
                      "const": "unchanged"
                    }
                  }
                }
              }
            }
          }
        }
      ]
    },
    "method": {
      "description": "An implementation method represented for its class.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name",
        "changeType"
      ],
      "properties": {
        "userFlow": {
          "$ref": "#/$defs/userFlow"
        },
        "name": {
          "type": "string",
          "minLength": 1
        },
        "changeType": {
          "$ref": "#/$defs/changeType"
        }
      }
    },
    "stateVariable": {
      "description": "A mutable instance field represented for its class. State relationships reference the exact state-variable entry.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name",
        "changeType"
      ],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1
        },
        "changeType": {
          "$ref": "#/$defs/changeType"
        },
        "userFlow": {
          "$ref": "#/$defs/userFlow"
        }
      }
    },
    "exposedVariable": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name",
        "kind",
        "declaredAt"
      ],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1
        },
        "kind": {
          "enum": [
            "instance",
            "parameter",
            "local"
          ]
        },
        "method": {
          "description": "Existing method containing the parameter or local.",
          "type": "string",
          "minLength": 1
        },
        "declaredAt": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "file",
            "line",
            "column"
          ],
          "properties": {
            "file": {
              "description": "Canonical repository-relative path to the declaration in current code.",
              "type": "string",
              "minLength": 1
            },
            "line": {
              "type": "integer",
              "minimum": 1
            },
            "column": {
              "type": "integer",
              "minimum": 1
            }
          }
        }
      },
      "allOf": [
        {
          "if": {
            "properties": {
              "kind": {
                "enum": [
                  "parameter",
                  "local"
                ]
              }
            }
          },
          "then": {
            "required": [
              "method"
            ]
          }
        },
        {
          "if": {
            "properties": {
              "kind": {
                "const": "instance"
              }
            }
          },
          "then": {
            "not": {
              "required": [
                "method"
              ]
            }
          }
        }
      ]
    },
    "classEndpoint": {
      "description": "References a class by its document-unique name.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "class"
      ],
      "properties": {
        "class": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "methodEndpoint": {
      "description": "References a method by its class name and class-local method name.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "class",
        "method"
      ],
      "properties": {
        "class": {
          "type": "string",
          "minLength": 1
        },
        "method": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "stateVariableEndpoint": {
      "description": "References a state variable by its class name and class-local state-variable name.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "class",
        "stateVariable"
      ],
      "properties": {
        "class": {
          "type": "string",
          "minLength": 1
        },
        "stateVariable": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "componentEndpoint": {
      "description": "References a component by its document-unique name.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "component"
      ],
      "properties": {
        "component": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "staticDataEndpoint": {
      "description": "References fixed data by its document-unique name.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "staticData"
      ],
      "properties": {
        "staticData": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "dataflowEndpoint": {
      "description": "A method or component that directly sends or receives data.",
      "oneOf": [
        {
          "$ref": "#/$defs/methodEndpoint"
        },
        {
          "$ref": "#/$defs/componentEndpoint"
        }
      ]
    },
    "relationshipType": {
      "oneOf": [
        {
          "const": "dataflow",
          "description": "A direct data transfer between method or component endpoints, or a read from static data by a method. System-input components may only be sources and system-output components may only be targets. Record request and response paths as separate relationships."
        },
        {
          "const": "state-read",
          "description": "A method reads a value from mutable instance state; the state variable is the source and the method is the target."
        },
        {
          "const": "state-update",
          "description": "A method writes mutable instance state; the method is the source and the state variable is the target."
        },
        {
          "const": "composition",
          "description": "A class owns a child class. A field used only for this ownership is represented by composition rather than as a state variable."
        }
      ]
    },
    "relationship": {
      "description": "A direct architectural relationship between two represented endpoints.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "from",
        "to",
        "type",
        "changeType"
      ],
      "properties": {
        "userFlow": {
          "$ref": "#/$defs/userFlow"
        },
        "from": {
          "description": "Explicit source reference, constrained by relationship type.",
          "type": "object"
        },
        "to": {
          "description": "Explicit target reference, constrained by relationship type.",
          "type": "object"
        },
        "type": {
          "$ref": "#/$defs/relationshipType"
        },
        "dataDescription": {
          "description": "What data this relationship transfers, reads, or modifies.",
          "type": "string",
          "minLength": 1
        },
        "purpose": {
          "description": "Why this relationship is needed. On a user-flow relationship, its role in the flow, naming a step where one depends on it; otherwise why the relationship exists at all.",
          "type": "string",
          "minLength": 1
        },
        "changeType": {
          "$ref": "#/$defs/changeType"
        }
      },
      "allOf": [
        {
          "if": {
            "properties": {
              "type": {
                "const": "state-read"
              }
            }
          },
          "then": {
            "required": [
              "dataDescription",
              "purpose"
            ],
            "properties": {
              "from": {
                "$ref": "#/$defs/stateVariableEndpoint"
              },
              "to": {
                "$ref": "#/$defs/methodEndpoint"
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "dataflow"
              }
            }
          },
          "then": {
            "required": [
              "dataDescription",
              "purpose"
            ],
            "oneOf": [
              {
                "properties": {
                  "from": {
                    "$ref": "#/$defs/dataflowEndpoint"
                  },
                  "to": {
                    "$ref": "#/$defs/dataflowEndpoint"
                  }
                }
              },
              {
                "properties": {
                  "from": {
                    "$ref": "#/$defs/staticDataEndpoint"
                  },
                  "to": {
                    "$ref": "#/$defs/methodEndpoint"
                  }
                }
              }
            ]
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "state-update"
              }
            }
          },
          "then": {
            "required": [
              "dataDescription",
              "purpose"
            ],
            "properties": {
              "from": {
                "$ref": "#/$defs/methodEndpoint"
              },
              "to": {
                "$ref": "#/$defs/stateVariableEndpoint"
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "type": {
                "const": "composition"
              }
            }
          },
          "then": {
            "not": {
              "anyOf": [
                {
                  "required": [
                    "userFlow"
                  ]
                },
                {
                  "required": [
                    "dataDescription"
                  ]
                },
                {
                  "required": [
                    "purpose"
                  ]
                }
              ]
            },
            "properties": {
              "from": {
                "$ref": "#/$defs/classEndpoint"
              },
              "to": {
                "$ref": "#/$defs/classEndpoint"
              }
            }
          }
        }
      ]
    }
  }
}
